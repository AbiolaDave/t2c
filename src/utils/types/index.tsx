

export type CurrencyOption = {
  code: string;
  name: string;
  symbol: string;
  flag: string; 
};

export type RatesProviderType = {
        id: number,
    name: string,
    logo: string,
    abbr: string,
    rate: string,
    price: number,
}